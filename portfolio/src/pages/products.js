import React, {Component} from 'react';
import Layout from '../components/Layout';

class Products extends Component {
    render() {
        return (
            <Layout>
                <h1>buy my running fuel</h1>
            </Layout>
        );
    }
}

export default Products;
